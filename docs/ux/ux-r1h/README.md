# UX-R1H Weekly Operations Evidence

These screenshots are generated by `e2e/ux-r1h-weekly-operations.spec.ts` with `UX_CAPTURE=1`.

- `weekly-manager-desktop.png`: a commercial manager creates owned weekly work inside the selected execution plan.
- `weekly-specialist-mobile.png`: the assigned specialist updates their work on a mobile viewport.
- `weekly-cco-approval-desktop.png`: the CCO sees and completes the governed approval step.

The Playwright suite also covers keyboard activation, the empty state, the load-failure state, role boundaries, API failures, console failures, and horizontal overflow.

