global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: tanaghum-backend
    metrics_path: /ops/prometheus
    bearer_token_file: /run/secrets/ops_metrics_token
    static_configs:
      - targets:
          - app:4000
